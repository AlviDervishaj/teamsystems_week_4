export type TodoType = {
  title: string,
  description: string,
  id: string,
  completed: boolean,
  userId: string,
}

export type UserType = {
  username: string,
  email: string,
  password: string,
  id: string,
}
